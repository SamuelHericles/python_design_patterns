# STATE

Alerta comportamento quando estado interno muda. Parece mudar de classe. Máquina de estados - comportamento muda por estado. Compormento depende do estado. Muitos ifs por estado atual pode ser alterado por um conjunto de classes intercambiáveis. TCP Connection, document workflow.

```
state/
├── state.py
├── concrete_states.py
├── context.py
└── main.py
```